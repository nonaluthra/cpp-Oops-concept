//Constructors
/*Properties of constructor
1.Constructor name should be same as class name
2.It is automatically invoked(called) when object is created
3.Always declared in public section of class
4It oes not return any value and have no return data type
*/
#include <iostream>

using namespace std;
class Complex{
    int a,b;
    public:
    Complex(void);                    //constructor declaration-this is default constructir as it does not contain any arguments
    void printNumber(void){
        cout<<"the number is "<<a<<"+"<<b<<"i";
    }
};
Complex::Complex(void){
    a=10;
    b=5;
}
int main()
{
  
Complex c;
c.printNumber();
    return 0;
}
